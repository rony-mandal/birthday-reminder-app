services:
  # Backend Service
  - type: web
    name: birthday-reminder-backend
    env: python
    region: oregon
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MONGO_URL
        sync: false
      - key: DB_NAME
        value: birthday_reminder
      - key: CORS_ORIGINS
        value: "*"

  # Frontend Service  
  - type: web
    name: birthday-reminder-frontend
    env: static
    region: oregon
    plan: free
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: build
    envVars:
      - key: REACT_APP_BACKEND_URL
        value: https://birthday-reminder-backend.onrender.com
